name: "test-08 config absent silent skip"
on:
  push:
  workflow_call:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: snowflakedb/snowflake-actions@feat/modular-install-cli-cortex
      - run: snow --version
